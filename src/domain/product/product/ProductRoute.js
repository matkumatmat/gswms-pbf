// src/domain/product/product/ProductRoute.js

/**
 * PANDUAN ROUTING!
 * Copy blok kode di bawah ini dan paste ke dalam `DomainRegistry` yang ada di file `src/main.js`
 */

/*
  'getProducts': {
    factory: () => new ProductService(new ProductRepo()),
    cacheGroup: AppConfig.DB_PRODUCT_SHEET_NAME
  },
*/

/**
 * Kalau butuh nambah/edit data dari frontend, masukin ini ke `PostRegistry` di `main.js`:
 */

/*
  'createProduct': {
    factory: () => new ProductService(new ProductRepo()),
    method: 'create'
  },
*/